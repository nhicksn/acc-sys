// Account System with a username and password
// Includes registration process
// Will remember multiple accounts within a single use
// Reprompts user until password is correct, or user exits

#include "system.h"
#include <iostream>

using namespace std;

int main() {
    database data("database.txt");

    return 0;
}